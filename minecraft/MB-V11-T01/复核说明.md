# T01 复核与复现

本目录是任务成果，不是正式资产库。游戏世界保留在本机，未上传完整存档或备份。

- `园林生成.mjs`：只生成 Canonical Blueprint 分块与阶段清单。
- `阶段施工.mjs 1`：仅在指定测试路径不存在时创建世界；2、3、4 执行后续批次。交付世界已经存在，不得再次执行阶段 1。
- `局部修正.mjs`：根据本任务世界生成局部修正；本次实际执行内容以已保存的 `修正批次.json` 为准。
- `存档审计.mjs 4`：从公开只读接口核验元数据、指定方块和目的地连通性。
- `空间绘制.py actual`：从恢复后的 `存档回读方块.json` 绘制证据，需要 Python、NumPy、Pillow 及微软雅黑字体。

Node 脚本在原 `AI工程/MB-V11-T01` 目录使用相邻正式工程 L3 接口，不是独立安装包。所有写入使用 AI-Offline 正常加载和 Minecraft 保存，不使用 raw region 写入。

发布包的 `存档回读方块.json.gz` 是无损压缩的实际回读数据，范围 X=-3..187、Y=58..103、Z=-3..153，每行是 `[x,y,z,state]` 非空气方块；不是完整世界。包络外没有读取，不应推断为空气。恢复方式：

```python
import gzip
from pathlib import Path
p = Path('存档回读方块.json.gz')
p.with_suffix('').write_bytes(gzip.decompress(p.read_bytes()))
```

使用 SHA256SUMS.json 中的 uncompressed_sha256 核验恢复结果。公开范围不含完整游戏存档、备份、凭据、第三方 Mods 或研究网页全文。
