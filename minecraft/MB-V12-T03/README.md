# MB-V12-T03 河谷村落

阅读 [Completion Report](Completion%20Report.md)。本目录是施工、研究与独立空间审计成果，不含完整存档或备份。执行器原始 JSON 中 `status: PASS` 仅为该次保存/采样的工具状态，不是本轮回归结论。

## 证据恢复与审计

`证据/阶段*.bin.gz` 为无损 gzip；解压后每字节是同名 JSON 的 palette 索引。尺寸256×80×256，轴序 X/Y/Z，Z最快，原点(0,56,0)。原始 bin SHA256 在 JSON 中；归档文件校验见 SHA256SUMS。终态是 **阶段3c**。

用 Python gzip 解压到同目录 `.bin`，运行 `几何检查.py 3c`、`空间观察.py 3c` 可复算几何与图像；需 NumPy、Pillow、Node，观察脚本字体路径按本机调整。无需打开或写入游戏存档。最终透视是存档区块的软件几何观察，非客户端截图。

原始期望数组未归档。全部精确施工目标保存在 gzip Canonical Blueprint 和对应作业中；检查终态时以这些确定数据为准。阶段顺序：`01 → 1b → 1c → 02 → 2b → 03 → 3b → 3c`，不能跳过补修阶段，也不能仅重新运行最初生成器后跳过修订。

`生成河谷.py` 与修订脚本保留本轮设计来源和过程。部分修订依赖前阶段 `.npy` 及场景元数据，部分生成依赖本机 REF-0049；它们不是脱离原工作区的一键安装包。完整重放采用已归档 Canonical Blueprint/作业，正式执行器依赖 `AI-Offline`、`AI-Preview`、`AI-Blueprints` 的公开接口，未打包运行时与 Mods。现有脚本拒绝在已存在目标目录上再次新建，禁止为重放覆盖本次存档；任何新的施工需另行明确目标授权。

`世界作业.mjs` 绑定唯一测试世界，持锁备份/加载/保存/关闭交由既有离线公开接口。无需也不应使用 raw region 写入。

## 归档范围

保留原工作目录 `D:/Games/Minecraft/AI工程/MB-V12-T03`；发布目录仅本任务。包含两项实际使用研究快照、原 Skill 快照、蓝图、作业、执行/身份/几何证据、各阶段实存图和压缩读回体素。排除未采用的404页面、临时渲染文件、完整游戏存档、备份及无关内容。

推荐入库资产候选：本轮无推荐入库资产候选。未作正式入库、未判回归 PASS/FAIL、未执行 T04。
