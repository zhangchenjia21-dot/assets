# MB-V13-T04 山脊城堡

先读 [Completion Report](Completion%20Report.md)。这是独立回归施工及空间证据归档，不是回归 PASS/FAIL 判定。执行器原始 JSON 的 `status: PASS` 只表示该次执行/采样状态。

## 只读恢复与审计

终态为 **03a**。`证据/03a.bin.gz` 无损解压后是256×128×256个 uint8 palette 索引，X/Y/Z轴序、Z最快，原点(0,56,0)。对应 palette、原始 SHA256 在 `证据/03a.json`。`阶段*.npy.gz` 是本任务同范围期望模型，解压后可供精确比对。不是完整 Minecraft 存档。

对所有 `.bin.gz` 和 `.npy.gz` 用标准 gzip 解压到同目录去除 `.gz` 后缀的文件。Python需NumPy/Pillow，Node需可用；字体路径见 `生成观察.py`，可按本机修改。运行 `空间检查.py 03a` 和 `生成观察.py 03a` 即可重新做只读审计，无需加载或写入游戏存档。运行 `阶段接口检查.py` 可复查02a→03的实际系统接口。

原始数据的哈希位于对应JSON，所有归档文件的SHA256位于 `SHA256SUMS`。归档时逐一验证压缩文件无损往返。

## 施工复现说明

实际顺序 **01 → 02 → 02a → 03 → 03a**。各阶段 Canonical Blueprint 和等价批操作作业均保留，施工前再次逐格检查二者相等。`生成山脊.py` 表达原始设计；`门楼地表修订.py`、`主塔封顶修订.py` 表达本轮修订。归档已包含各阶段确定数组与作业，不必为了观察重新运行施工生成器。

`离线施工.mjs` 使用既有 AI-Offline、AI-Preview、AI-Blueprints 的 L3 公开接口。运行时、Mods、账户配置和完整存档/备份不在归档中。脚本仅绑定本轮世界；新建作业会拒绝覆盖已有世界。不能为了复现删除本次存档或把路径改为正式世界，新的写入目标必须另行明确。

原工作目录保留在 `D:/Games/Minecraft/AI工程/MB-V13-T04`；来源目录只包括本轮实际使用的Skill和两项机构史料快照。未读取或采用其它测试审核结论。无正式资产入库，完成T04后停止。
