# MB-V17-T09 归档

先读 [Completion Report](Completion%20Report.md)。本包包含实际世界采样证据，不包含完整 Minecraft 存档；不自行给出回归 PASS / FAIL。

`蓝图与实存数据.zip` 无损保存各阶段蓝图、设计数组和实存数组。解压至本目录，保留相对路径；用 `SHA256SUMS.txt` 校验归档文件。ZIP 自带 CRC，归档脚本已执行完整性检查。

安装 Python、NumPy、Pillow 后，可运行 `python 空间核验.py 04-Repair` 重做静态检查；运行 `python 审计视图.py 04-Repair` 重绘透视、剖面和平面。字体依赖 Windows `msyh.ttc`。实存 u16 为小端、形状 `[56,144,144]`，坐标原点 `[0,10,0]`，调色板在对应 `实存.json`。

`生成浴场.py` 与 `世界作业.mjs` 保留施工复现依据。后者依赖原工作区 AI-Offline 公共接口，并硬绑定测试世界路径；不要将其当作独立通用导入器直接运行，也不要对已有世界重复执行 create。归档与离线视图复核均不需要写入世界。

证据图片是实存体素渲染，不是客户端截图。几何校验不能证明原生碰撞、光照或流体更新稳定性，具体限制见报告。
