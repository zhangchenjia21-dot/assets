# MB-V19-T11 京町家

先读 [Completion Report](Completion%20Report.md)，再打开 [同机位空间审计](同机位空间审计.html)。GitHub 不直接预览 HTML，可下载本目录本地打开；图片也可单独查看。Scope 为 FINISHED，回归最终结论留给独立审核。

`蓝图与实存.zip` 为无损 ZIP，解压到本目录恢复 `蓝图/`、各阶段 `.npz` 和 `证据/*.u16`。CRC 与逐成员原文件内容已核对，归档文件 SHA256 见 `SHA256.json`。u16 为小端 YZX，shape=[36,96,56]，原点=[0,10,0]，调色板来自同名 `*-实存.json`。不含完整游戏世界、备份、凭据或 Mods。

只读复核：Python 需 NumPy、Pillow。执行 `空间核验.py 09-Restraint`、`阶段保护核验.py 09-Restraint` 和 `审计视图.py 09-Restraint`；字体路径默认 Windows 微软雅黑。`交付核验.py` 核对重载与 19 组视图，`整理证据.py` 依赖原本机作业日志才能重新收集原生日志摘要。只读审计脚本不会写游戏存档。

施工脚本为本机公开 AI-Offline 流程的复现记录，依赖工程根目录对应 L3 接口，不是可跨环境自动运行的独立安装器。`世界作业.mjs` 硬编码唯一 T11 世界且禁止覆盖已执行阶段；不要对正式世界运行，不要为了复现移除身份保护或修改登记配置。蓝图原点已经是世界坐标，不能将它当作相对资产随意贴入其它已有建筑。

当前 Skill v1.9 原文保留在研究目录，Git blob 已验证。历史资料只归档引用与设计提取；未复制公开网站整页与照片进发布仓库。没有读取既往回归评审或结论作为设计提示。
