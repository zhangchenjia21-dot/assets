# MP-P05R2 交付与审核说明

本轮完成新的 L4 组团规划、machine-readable planning data、两个 Builder Design Packages、六项局部接口、八项外部服务预留，以及三张坐标规划图（SVG + PNG）。状态为等待 GPT + Owner 审核。没有执行 Builder Architecture Design，没有作本轮回归 PASS / FAIL 判断。

## 本轮实际来源

Skill：minecraft-planner v0.5。Skill source commit：`fc6371361685e2eeaefdef5a513f21dbe64c6696`，`zhangchenjia21-dot/Vibe-Coding`。

共享契约：Minecraft Planner–Builder Contract v1.0，同一提交的 `skill/codex/shared/minecraft-planner-builder-contract.md`。GitHub main-to-main compare用于确认提交，再在该提交读取文件；Skill与契约在开始L4规划前已记录于 `provenance.json`。没有修改、升级或重新安装Skill。

父规划输入为 MP-P04/r1 PACKAGE-01、P01/P02及其frontage、BLOCK-01、直接公共协调PACKAGE-03、SPACE-01、LANE-01/02/04。只提取这些对象，未向模型展开整个父包数组。Canon使用当前CIV-001入口、D-014与其授权Grammar；没有读取任何既有具体建筑设计。

## 阅读边界

- 未进入、读取或修改旧 MP-P05R 目录；未在其下建立子目录。
- 未读取 MP-P05、MP-I01 的文件，未读取相关 Independent Review。
- 未读取其它既有 URBAN_ENSEMBLE / Builder 项目答案。
- 当前Skill自带的通用说明与JSON例属于允许读取的Skill方法资料；没有根据其中的历史任务名追读项目文件。
- 父包引用的其它上游供给规划没有被扩展读取。只记录当前许可范围内已声明的外部依赖，并给出具体责任与闭合阶段。
- 初始阶段看到的旧目录名/文件名已获Owner允许继续；它们未用作本轮空间答案。

脚本 `证据提取.mjs` 不递归搜索资产目录，以明确路径读取允许的源文件，并在数据层筛选对象。原始地表/近地数据只归档本地场地与直接公共接口的裁切；本轮未读取玩家文件、容器库存或整份存档。

## 检查证据

`validation.json` 记录50项资料一致性检查、0项不满足：两户/269柱总量；小院、apron与建筑探索区完整且互斥；私地与公共mask不重叠；门前与公共面邻接；局部接口字段与依赖哈希可解析；源文件与相关世界文件哈希一致；图件存在。

另记录两户建筑搜索区均为单个四邻接连通区域，公共并集内可容纳2×2平面方形的候选共有606处且连通。它仅说明有连续平面空地供联合设计研究，不等于沿名义中心线已经具备最终净宽、平整踏步或实机移动能力。

三张PNG经目视检查，中文、坐标、图例、尺度、公共/户用区别与剖面可读。上沿入口优先段的现状顶面明确为Y136；纵剖的Y是地面方块顶面，避免与ground_y索引混淆。没有图像生成式地形或建筑臆造。

复现检查只涵盖本轮数据与图件。它不是独立审核、施工回读、Builder功能验收或回归裁定。

## 下游仍需处理

两个包为 `CONCEPT_DESIGN_READY`，能够开始Architecture Design；并非 `DESIGN_FREEZE_READY`。

1. 设计冻结前：公共接口责任方接受局部净宽/接触标高与雨水受纳责任；Builder证明家庭、修理/复核、短宿、卫生及真实通路尺度；核对具体基础影响体积。
2. 世界写入前：确认目标、权属、地役、保护体积和revision/hash绑定许可。
3. 常住/运行前：确认食水持续能力、洁污错峰和实际清运终端。

本轮没有执行上述下游行为，也没有用未知替代已给出的局部几何。何时应将接口回父层修订，见 `handoff-register.json`。

## 写入与归档边界

`world writes = 0`。没有启动客户端、服务器、执行器；没有修改Minecraft存档、既有规划、Canon或Skill。源世界只做相关文件的只读哈希核验；该核验不宣称整个存档的其它文件从未被外部进程改变。

归档只写 `minecraft/MP-P05R2-NORTH-FRONTAGE-ENSEMBLE/`。远端存在本轮以外的新提交；提交基于已fetch的远端提交，用独立临时Git index加入本轮新目录，形成正常子提交。这个过程不检出、不读取、不改写禁止目录的工作区文件，也不覆写远端已有内容。推送不使用force。

完成归档后即停止，交 GPT + Owner 审核；本轮不创建独立审核文件，不自行推进Builder或施工。
